package org.tywrapstudios.tempban.command

import org.bukkit.command.Command
import org.bukkit.command.CommandSender
import sun.misc.Unsafe

class TempbanCommand : Command("tempban") {
    override fun execute(sender: CommandSender, currentAlias: String, args: Array<String>): Boolean {
        TODO("Waiting for griinzy API I guess")
    }
}